apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "cobbler-web.labels" . | nindent 4 }}
  name: {{ include "cobbler-web.fullname" . }}
spec:
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "cobbler-web.selectorLabels" . | nindent 4 }}
  type: {{ .Values.service.type }}
